#include <iostream>
using namespace std;
int main()
{
    // Multipe table for the number upto five places
    int num;
    cout << "Enter any number:";
    cin >> num;
    cout << "The multiples are:" << num << " " << num + num << " " << num + num + num << " " << num + num + num + num << " " << num + num + num + num + num;
}
